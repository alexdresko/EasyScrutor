# Auto detect text files and perform LF normalization
* text=auto

# Force C# files to use CRLF line endings (to match .editorconfig)
*.cs text eol=crlf
*.csproj text eol=crlf
*.sln text eol=crlf

# Force shell/config files to use LF
*.sh text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
